// components/Heading.jsx
import React from "react";

const Heading = () => {
  return (
    <Heading>
      <h1>Price Estimation Tool Web Application development.</h1>
      <p>We use an iterative method to develop web applications, so we only provide fixed estimates on small pieces of work at a time.</p>
      <p>You also only pay for working software.</p>
      <p>This tool is just to provide a guide towards costs.</p>
    </Heading>
    
  );
};

export default Heading;